// This config is to do type checking in our files while running tests.
{
  "extends": "./tsconfig.json",
  "include": ["./tests", "./src/svg.d.ts"],
  "exclude": ["node_modules"],
  "compilerOptions": {
    "noEmit": true,
    "types": ["jest", "node"]
  }
}